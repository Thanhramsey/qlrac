# Môi trường Local Dev (Mặc định dùng Vite proxy /api -> http://localhost:3000)
VITE_API_BASE_URL=/api

# Bỏ comment dòng dưới nếu muốn Frontend local gọi thẳng tới Railway backend:
# VITE_API_BASE_URL=https://qlrac-production.up.railway.app
